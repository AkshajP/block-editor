To run the project you need-

```sh
./scripts/bootstrap.sh
npm i -g pnpm
pnpm i
```

```sh
pnpm run dev
```

You can see the edits show in pgadmin via docker.
the server is automatically mounted, only the password needs to be entered
