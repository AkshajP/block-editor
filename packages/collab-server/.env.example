# PostgreSQL — must match docker-compose.yml credentials
PGHOST=localhost
PGPORT=5432
PGDATABASE=collab
PGUSER=postgres
PGPASSWORD=postgres

# Shared HMAC secret for verifying WS access tokens — must match apps/web WS_SECRET
WS_SECRET=dev-ws-secret-change-in-prod
